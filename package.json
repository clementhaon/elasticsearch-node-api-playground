{
  "name": "elasticsearch-node-api-playground",
  "version": "1.0.0",
  "description": "A repository designed for testing Elasticsearch search functionality on a large volume of synthetic/fake data",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@elastic/elasticsearch": "^8.11.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
